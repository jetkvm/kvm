#!/bin/sh

# Skip if lint-staged isn't available (e.g. Windows Git GUI running outside WSL)
if ! command -v npx >/dev/null 2>&1; then
  echo "husky: npx not found, skipping pre-commit hook (run lint manually)"
  exit 0
fi

cd ui && npx lint-staged
